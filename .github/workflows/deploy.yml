name: Deploy
on:
  push:
    branches: [ main, master ]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2
      - name: Set up Buildx
        uses: docker/setup-buildx-action@v2
      - name: Build images
        run: docker-compose -f docker-compose.yml build
      - name: Deploy (stub)
        run: |
          echo "Deployment step would run here (e.g., push to registry, apply k8s manifests, etc.)"
